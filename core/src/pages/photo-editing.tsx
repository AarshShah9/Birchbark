import type {NextPage} from "next";
import PhotoEditingPage from "../pages/templates/PhotoEditingPage";

const PhotoEditing: NextPage = () => {
    return <PhotoEditingPage/>;
};

export default PhotoEditing;
